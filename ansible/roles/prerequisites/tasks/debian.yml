---
- name: Update apt cache
  ansible.builtin.apt:
    update_cache: yes
    cache_valid_time: 3600
  become: yes

- name: Install build essentials
  ansible.builtin.apt:
    name:
      - build-essential
      - curl
      - wget
      - git
    state: present
  become: yes
