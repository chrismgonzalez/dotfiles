-- bootstrap lazy.nvim, LazyVim and your plugins
-- uncomment the below to lines to revert to the pre-existing config
-- require("{directory}.{fileName.lua}
require("config.lazy")
require("config.zettelkasten")
