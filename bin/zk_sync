#!/usr/bin/env sh

ZK_PATH="/Users/chris/zettelkasten"
cd "$ZK_PATH"

CHANGES_EXIST="$(git status --porcelain | wc -l)"

if [ "$CHANGES_EXIST" -eq 0 ]; then
  exit 0
fi

git add .
git commit -q -m "Automated commit at: $(date)"
git pull
git push -q
