function awsp() {
    if [ -z "$1" ]; then
        echo "Please provide an AWS profile name"
        return 1
    fi
    export AWS_PROFILE="$1"
    echo "AWS Profile set to: $AWS_PROFILE"
}

awsp()


